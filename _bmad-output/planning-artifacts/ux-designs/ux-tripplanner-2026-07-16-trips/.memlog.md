---
topic: Trip Planner — Trips list (/trips) + Trip detail (/trips/:id) UX redesign (extends Horizon)
updated: 2026-07-16T00:00
---

- (event) run created via bmad-ux Create intent; no python/uv runtime on machine, memlog maintained by hand in script format (mirrors ux-tripplanner-2026-07-15 convention)
- (note) folder suffixed `-trips` to avoid clobbering the finalized ux-tripplanner-2026-07-16 (Azure attraction-detail) run of the same date
- (insight) live app ships the Horizon token system verbatim in FE/src/index.css (primary #0058bc, Plus Jakarta Sans, Material-3-style surface ramp, spring easing). These two surfaces must EXTEND Horizon, not introduce a new identity. The Azure run (2026-07-16) was a separate exploration and is NOT the app's shipped language.
- (insight) current /trips (TripsPage.tsx): plain auto-fill card grid; each card = name + date range + "N days" chip; header "My trips" + New trip button; skeleton/error/empty states already exist; staggered card-enter animation.
- (insight) current /trips/:id (TripPlannerPage.tsx): header name + date range + Edit trip button; vertical stack of Day sections; each day = date heading + rows of destinations (name + category chip + StarRating + Remove); empty-day text; ConfirmDialog for removal; not-found / loading / error states exist.
- (insight) data shape: Trip{id,name,startDate,endDate,tripDays[]}; TripDay{day,destinations[]}; Destination{id,name,rating(number),category,xid,openingHours,cuisineType,isHalalFriendly}. No cover image, no location/city field on Trip today. xid!==null destinations link to /attractions/:xid.
- (decision by user) working mode: COACHING path — walk decisions together, render HTML options/wireframes as we go
- (decision by user) ambition: REFINE, STAY LEAN — keep current IA/structure; elevate spacing, hierarchy, card polish, states, micro-interactions; NO new data fields and NO backend dependencies (nothing that would require a Trip cover image, city/location, or reorder persistence)
- (event) creative tool: rendered .working/directions-3.html — 3 side-by-side directions (Boarding Pass / Editorial Calm / Dashboard) across both /trips and /trips/:id, Horizon tokens, real data
- (decision by user) chosen direction: DIRECTION 1 — BOARDING PASS. Trip = travel-pass card (name, departure→return route line, perforated stat stub); detail = itinerary of day "segments" on a vertical journey rail with date nodes. Warm, on-theme, characterful.
- (decision by user) pass card shows STATUS + COUNTDOWN pill derived from dates vs today (2026-07-16): future="In N days", ongoing="Ongoing · Day k", past="Past". Ties tokens: upcoming=secondary-container, ongoing=primary/success, past=muted surface.
- (decision by user) detail page: each day segment gets a per-day "+ Add destination" entry point. Realistic lean flow: it navigates to the search/attractions surface to find a place; the EXISTING requestAdd → AddToTripDialog (pick trip → pick day) completes the add. No backend change. OPTIONAL nicety (not required): deep-link the search entry with tripId+day to preselect in the dialog — pure FE wiring, flagged implementation-optional so it never ships as dead UI.
- (decision by user) detail summary "ticket" header shows: date range · total days · total places · empty-day hint ("N days unplanned"; hidden/positive-toned when 0). Avg-rating stat NOT included.
- (insight) add-to-trip flow (AddToTripContext + AddToTripDialog): requestAdd(xid) opens modal to pick trip then day; xid originates from an attraction, so any "add" affordance must first route the user to find an attraction. Confirms the per-day add must go via search.
- (event) creative tool: rendered .working/keyscreen-boarding-pass.html — refined single-direction key screens for both surfaces with all decisions baked in (status+countdown pills, summary ticket w/ empty-day hint, journey rail segments, per-day add, empty state)
- (decision by user) LOCK Boarding Pass as final. No further visual tweaks.
- (decision by user) list order: KEEP API order — do NOT add FE sorting (chose "Lock it" over "Lock + sort"). Status pill works regardless of order.
- (decision) past trips shown de-emphasized (~82% opacity) so upcoming trips lead; accepted implicitly under lock.
- (decision by user) reviewer gate at finalize: RUBRIC WALKER + ACCESSIBILITY lens (parallel subagents), findings resolved before close.
- (event) verified WCAG ratios for new pairings (node): upcoming pill onSecCont/secCont 4.58; ongoing success/successCont 5.49; past pill onSV/surface-container 7.98; segCount pill 4.58; unplanned-hint secondary/white 6.43; stat VALUE onSurf/white 17.17; chip onSV/surface-variant 7.22; remove error/errCont 5.00; addBtn primary/bg 6.40 & /sLow 6.10; segDate primary/white 6.73.
- (decision, spine-over-mock) small uppercase stat/stub LABELS use on-surface-variant (8.44:1 on surface-container-low), NOT outline (4.07:1 fail). outline stays icons/hairlines only.
- (decision, spine-over-mock) "Past" pill uses on-surface-variant on surface-container (7.98:1), not outline (3.84:1 fail).
- (decision, spine-over-mock) DROP the mock's blanket 82%-opacity dimming of past passes — opacity degrades text contrast below AA. De-emphasize past trips via the muted pill + a desaturated (outline-variant) accent spine instead of the blue→teal gradient; all text stays full contrast.
- (event) reviewer gate ran (parallel subagents): review-rubric.md (0 crit / 1 high / 2 med / 8 low) and review-accessibility.md (0 crit / 2 high / 4 med / 4 low)
- (change) resolved rubric HIGH: defined {components.outline-button} (shipped .edit/.retry style) and repointed summary-ticket Edit button to it; added {components.trips-grid} Components row
- (change) resolved a11y HIGH: post-removal focus is now deterministic (day <h2> or add-day button; never <body>); resolved a11y HIGH: inline removal-failure banner restated with role="alert"
- (change) resolved a11y MED: added {components.today-marker} "Today" pill + "— Today" folded into day <h2> accessible name so today is never color-only; updated Derived Values, Accessibility Floor, Voice, Day-segment pattern
- (change) synced promoted mockup to final spine: stat labels on-surface-variant (not outline), past pass de-emphasized via muted pill + desaturated spine (no opacity dimming)
- (change) folded remaining review items: aligned typography frontmatter to parent schema (fontSize/fontWeight/letterSpacing/textTransform); dropped dead rounded.xl; added pass-name/route ink-on-white ratio row to Colors; route dates joined by visually-hidden "to" (range semantics); Remove/add-day 44px goal + hard ≥24px AA floor, attraction-name link claims inline 2.5.8 exemption; pass-stub Use cell corrected to "Pass stub"; add-day-button name-vs-label note added; mock caption softened to "primary states" + mock noted aligned to final tokens
- (note) intentionally left as low/optional: create→detail micro-flow, payload-shape duplication across spines, middot SR read, optional aria-live on unplanned hint, Inspiration section for rejected directions — all non-load-bearing
- (event) validation-report.html + .md synthesized (rubric + accessibility); all HIGH + MEDIUM resolved, LOWs triaged
- (note) mock coverage: /trips (populated+empty) and /trips/:id (loaded+empty-day) mocked; loading/error/not-found/editing are spine-only preservation contracts — no new mock needed
- (event) spines finalized (status: final, updated 2026-07-16)
