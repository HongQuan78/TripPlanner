---
topic: TripPlanner login/register UI redesign (Horizon design language)
updated: 2026-07-15T00:00
---

- (event) run created via bmad-ux Create intent; no python/uv runtime on machine, memlog maintained by hand in script format
- (import) imports/sign-in-horizon-travel.html — user-supplied Horizon Travel sign-in reference (Material 3-style token palette, Plus Jakarta Sans, split hero-image + auth-card layout, Tailwind CDN mock)
- (note by user) intent: improve UI/UX of login and register pages in FE based on the Horizon sign-in design
- (insight) FE/src/index.css already defines the exact Horizon token palette from the reference (identical hex values, Plus Jakarta Sans, same radii); the gap is auth-page layout and richness, not theming
- (insight) current auth pages (LoginPage, RegisterPage, VerifyEmailPage) share AuthForm.module.css: plain centered card, no hero split, no input icons, no password visibility toggle
- (decision by user) omit reference features the backend cannot honor — no Remember me, no Forgot Password, no Google/Apple social login; adopt visual language only, no dead UI
- (decision by user) scope covers three surfaces: Login, Register (incl. registration-received success state), and Verify Email (verifying/verified/failed/resend states)
- (decision by user) hero panel uses a local licensed/free travel photo shipped in FE/src/assets with gradient overlay and welcome copy, per the reference
- (decision by user) fast path working mode: batch gaps, draft spines with [ASSUMPTION] tags, key-screen mocks at finalize
- (note) auth routes currently render inside AppLayout (sticky header + nav); reference mock explicitly suppresses nav/footer on transactional auth screens — flagged as assumption for the spines
- (note) backend auth behaviors that constrain UX copy: register returns generic message for fresh AND duplicate emails (anti-enumeration); unverified login fails with generic "Invalid email or password."; resend-verification has 60s cooldown that silently returns generic success
- (note) register client validation today: email regex + password >= 8 chars; login submits with pending state, ApiError message shown in form-level error container
- (event) spines drafted by distillation subagent; 9 [ASSUMPTION] tags placed
- (decision by user) all nine assumptions accepted as drafted
- (decision by user) user-facing brand spelling is "Trip Planner" (two words); code identifiers stay TripPlanner — wordmark copy corrected in both spines
- (decision by user) reviewer gate: run rubric walker plus accessibility lens
- (event) reconcile-sign-in-horizon-travel.md written: 26/31 mock ideas covered, 4 deliberately dropped (remember-me, forgot-password, social login, social divider)
- (decision) dark mode out of scope: mock's darkMode class hook not adopted; app ships light theme only, noted in DESIGN.md Colors
- (assumption) placeholders adopt mock convention (hello@example.com / bullet dots) in {colors.outline-variant}; examples, never instructions
- (assumption) input icons ship as inline SVG, not Material Symbols webfont; SVG source left to implementation story
- (decision) button hover stays {colors.primary-container} (brighter, matching existing AuthForm.module.css) rather than the mock's surface-tint darkening — deliberate adaptation
- (decision) input focus keeps the existing global 2px {colors.primary} outline rather than the mock's border-swap + 1px ring — deliberate adaptation
- (note) mock's custom checkbox recipe dropped as remember-me collateral; recipe remains in imports/sign-in-horizon-travel.html if a checkbox is ever needed
- (event) reviewer gate ran: review-rubric.md (1 high / 5 medium / 10 low) and review-accessibility.md (0 critical / 3 high / 5 medium / 4 low)
- (decision) all [ASSUMPTION] tags stripped from both spines — every one was ratified; rationale kept as plain decisions
- (decision) placeholders dropped entirely, overriding the earlier placeholder assumption: outline-variant on white computes 1.70:1, no token passes AA while reading placeholder-quiet; labels + icons identify fields
- (decision) pending button keeps primary fill with swapped label, aria-disabled + submit guard + visually hidden role=status; disabled gray wash reserved for truly inert controls
- (decision) hero gradient contract tightened: overlay holds >=65% opacity through the whole welcome-copy block; supporting line full-opacity white (mock's 90% dropped)
- (decision) password toggle signals state via changing accessible name only, no aria-pressed; mock's focus:outline-none on the toggle documented as a defect
- (decision) hero welcome copy is aria-hidden and the headline a non-heading element — resolves the decorative-vs-DOM-text conflict
- (decision) Verify surface uses one persistent role=status container with children changing in place; failure may use role=alert insertion
- (decision) Login performs no client-side validation — every submit goes to the API, one generic banner voice; Register keeps client validation (its rules reveal nothing about accounts)
- (decision) input hairline border at 1.70:1 accepted as a documented exception (label + icon + height + focus outline identify the field)
- (decision) shadows and motion (incl. ease-spring cubic-bezier(0.34, 1.56, 0.64, 1)) promoted into DESIGN.md frontmatter tokens
- (decision) inline import reference + spines-win-on-conflict statement added to both spines
- (note) accessibility floor marked as net-new work — current pages implement none of the ARIA contract; carries into story acceptance criteria
- (note) FE/src/assets does not exist yet; the implementation story must create it for the hero photo
- (event) key-screen mocks rendered and promoted to mockups/: key-login.html, key-register.html, key-verify-email.html — every IA surface mocked incl. alternate states; hero photo rendered as gradient stand-in
- (decision) card subtitle "Please enter your details to continue." on Login and Register; cross-link footers committed verbatim (Login "Don't have an account? Sign Up", Register "Already have an account? Sign In")
- (note) mock coverage complete — no spine-only surfaces remain
- (event) validation-report.html + validation-report.md synthesized from both reviews (0 critical / 4 high / 10 medium / 16 low, all findings applied to the spines before finalize)
- (event) editorial polish passes applied (structure + prose): provenance repetition trimmed to Brand & Style, mobile rule stated once, hero rationale deduplicated
- (event) spines finalized
- (event) run reopened via bmad-ux Update intent: improve home page (/ -> SearchPage) and attraction cards — user finds them "simple and boring"
- (insight) home page today: centered "Where to next?" h1, tagline, search bar + Search/Clear buttons, then location results and attraction card grid; no hero, no visual brand moment; pre-search state is nearly empty
- (insight) AttractionCard today: 9rem cover image (or emoji placeholder), name, star rating + optional heritage chip, up to 3 kind tags, ghost "Add to trip" button; card-enter stagger animation and hover lift already shipped
- (decision by user) home page gets a photographic hero band with gradient overlay; headline, tagline, and the search bar live inside the hero — extends the auth hero language to the home surface
- (decision by user) attraction cards stay image-above-body ("richer classic") but upgraded: taller image, rating badge floating on the image, heritage treatment, refined tags, full-width Add-to-trip action row
- (decision by user) first-visit (pre-search) state is in scope — page must not be blank before the first search
- (decision by user) fast path working mode again: batch gaps, draft spine updates with [ASSUMPTION] tags, key-screen mocks at finalize
- (insight) Attraction API payload carries distanceMeters (number|null) which the current card never displays — available for card richness without backend work
- (insight) LocationSearchResult carries name/countryCode/locationType/lat/lon — suggestion chips in the empty state can only trigger the existing text-search flow; there is no popular-destinations API
- (event) spine updates drafted by distillation subagent; 13 [ASSUMPTION] tags placed across both files
- (decision by user) all 13 assumptions accepted as drafted, incl. framed rounded hero band that persists on mobile, 12rem card image, distance line from distanceMeters, no-badge for unrated, pill kind tags, six curated popular-search chips that pre-fill-and-submit, disabled-empty Search kept as documented divergence
- (decision by user) reviewer gate for this update: rubric walker only (accessibility lens declined)
- (event) reviewer gate ran for the home update: review-rubric-home-update.md (0 critical / 1 high / 4 medium / 6 low)
- (event) all 11 findings + mechanical note resolved in the spines; all 13 [ASSUMPTION] tags stripped, rationale kept as plain decisions
- (decision) disabled Search commits the shipped 50%-opacity primary fill; button-primary.background-disabled token updated to match; {colors.disabled} gray reserved for inert chrome elsewhere
- (decision) Clear button takes a solid white fill inside the hero band (divergence from shipped transparent ghost, forced by the text-over-photography rule)
- (decision) heritage chip label committed as shipped lowercase "heritage"; announced once via card link text, no duplicate aria-label
- (decision) open suggestion dropdown overlays the pre-search chip row; chips never hide or reflow while typing
- (decision) spine pair name unified as "Trip Planner — Horizon"
- (event) key-screen mocks rendered and promoted to mockups/: key-home-presearch.html (first-visit hero band + chips), key-home-results.html (populated grid with all four card variants)
- (note) mock chip cities are illustrative only (Đà Nẵng, Paris, Tokyo, Rome, Barcelona, New York) — the curated list is an implementation-story constant
- (note) shipped AppLayout header renders "TripPlanner" one word; spine brand rule commits "Trip Planner" — one-word header copy needs fixing at implementation
- (note) mock coverage: home pre-search and populated grid mocked; attraction loading/error/empty and country-notice states stay spine-only — they ship today unchanged and are preservation contracts, not new builds
- (event) validation-report.html + validation-report.md re-synthesized for the home update (0 critical / 1 high / 4 medium / 6 low, all resolved before finalize; prior auth gate recorded as applied)
- (event) editorial polish passes applied (structure + prose): disabled-rationale, search-restore contract, typeahead ARIA list, and rating-badge contrast each consolidated to a single home; all five mocks enumerated in both spines
- (event) spines finalized for the auth + home scope
