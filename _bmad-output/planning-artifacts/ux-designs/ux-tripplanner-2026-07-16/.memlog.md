---
topic: TripPlanner attraction detail page redesign (fresh standalone run)
updated: 2026-07-16T00:00
---

- (event) run created via bmad-ux Create intent; no python/uv runtime on machine, memlog maintained by hand in script format
- (decision by user) fresh standalone run for the attraction detail surface, chosen over extending the finalized Horizon design system (ux-tripplanner-2026-07-15); design freedom for this page, Horizon tokens NOT inherited unless later requested
- (note) target surface: FE/src/pages/DestinationDetailsPage.tsx, route /attractions/:xid, example http://localhost:5173/attractions/Q3162511 (Q-prefixed id is a Wikidata QID)
- (note) current page structure: back link -> PhotoCarousel -> name + category -> description -> info rows (Address, Opening hours, Website) -> Add to Trip button + login hint; loading / error / not-found states present
- (note) data model DestinationDetails: xid, name, category (nullable), description (nullable), imageUrls[], address (nullable), openingHours (nullable), website (nullable), latitude (nullable), longitude (nullable); lat/lng currently unused (no map)
- (decision by user) stakes: consumer product — treat as a real shipping consumer travel product, polished, trustworthy, conversion-minded
- (event) source scan surfaced epic/epic-2-destination-details.md as candidate requirement for this surface (gitignored, local); pending user confirm to extract
- (decision by user) confirmed: extract epic-2-destination-details.md for required scope (behavior only; look/feel stays user-owned)
- (decision by user) overall mood: IMMERSIVE PHOTO-FIRST — full-bleed cinematic hero image, name/category overlaid, content flows below; Airbnb / premium-travel emotive feel
- (decision by user) content modules IN: interactive map (from lat/lng), open-now status badge (parsed from opening hours), nearby attractions rail
- (decision by user) content modules OUT (not selected): share/save affordance
- (decision by user) Add to Trip pattern: SIDE BOOKING PANEL — Airbnb-style persistent card in a right column on desktop holding Add to Trip + key facts; stacks on mobile
- (note) dependency risk: nearby-attractions rail may need a backend endpoint that does not exist yet; open-now needs parseable opening-hours data; both to verify
- (insight) epic-2 extracted: US1+US2 IMPLEMENTED as built. Map (US3) and dedicated opening-hours (US4) are explicit BACKLOG/deferred; DTO already carries lat/lng for future map. Nearby attractions and sharing/ratings: SILENT in epic (not required). Add-to-Trip must be disabled/unclickable when logged out (business rule). Detail view must still open with missing fields; images optional with placeholder; not-found -> NotFound.
- (insight) NEARBY RAIL IS FEASIBLE WITH EXISTING BACKEND: GET /api/locations/attractions?latitude=&longitude= (FE getAttractions -> Attraction[]) can be called with the current attraction's lat/lng. Attraction card data: xid, name, kinds[], rating (string|null), imageUrl, distanceMeters. Must filter out the current xid (self) and cap the list. No new backend endpoint needed. This redesign turns backlog US3 (map) into scope and adds a nearby rail on top of existing data.
- (insight) rating exists on Attraction (nearby/search results) but NOT on DestinationDetails; so nearby cards can show a rating, the main attraction cannot without a DTO change.
- (constraint) NO map library in FE/package.json. Interactive map requires a new dependency. Key-free options: Leaflet or MapLibre GL with OSM tiles (no API key). Keyed options (Mapbox/Google) need a token + billing. Decision needed.
- (constraint) opening hours is a raw provider string (OpenTripMap), format not guaranteed structured; reliable 'open now' computation may not be possible for all values. Need a graceful fallback when hours are unparseable or absent.
- (decision by user) map: Leaflet + OpenStreetMap tiles (react-leaflet, no API key), interactive pan/zoom with a marker; OSM attribution required
- (decision by user) open-now: always show raw opening-hours string; render 'Open now'/'Closed' badge ONLY when the string parses confidently; no badge when unparseable/absent (honesty over liveliness)
- (decision by user) form factor: responsive web, desktop + mobile; desktop = wide hero + right booking panel, mobile = single-column stack with sticky bottom Add-to-Trip action
- (decision by user) VISUAL DIRECTION CHOSEN: "Azure" — crisp coastal minimalism. Cool blue/teal on white, geometric sans (all-sans, no serif), tighter/scannable density, hairline borders, sharp small radii, low/flat shadow, confident solid primary button. Chosen from 3 rendered directions in .working/directions-attraction-detail.html (Terra/Azure/Lumen).
- (decision by user) named-protagonist journey ratified: "Linh, 28, lunch break, laptop." Searches Ninh Binh -> lands on Trang An detail page -> full-bleed hero hooks her -> reads short description, sees UNESCO + 'Open now' pill -> scrolls to map to gauge distance from guesthouse -> nearby rail surfaces Mua Cave Viewpoint (5.4km, 4.7) as a mental note -> CLIMAX: the persistent side booking panel that followed her down the page makes 'Add to Trip' a one-tap decision at the moment of conviction. Added to her Ninh Binh itinerary.
- (event) Discovery closed; user green-lit distilling both spines. No user-supplied imports (imports/ empty).
- (event) reviewer gate ran (rubric + accessibility). Rubric: 0 critical / 1 high / 2 medium / 6 low; verdicts mostly strong/adequate. Accessibility: 5 critical / 4 high / 5 medium / 4 low.
- (insight) convergent blocker: Azure palette fails WCAG AA contrast on white — na #9aa7b6 (2.45), star amber #f0a92c (2.02), label-muted #7a8aa0 (3.4), teal #12a3a3 (3.09 @12px), open-now #1f9d5b (3.11); frontmatter falsely claimed AA-verified. Hero eyebrow sits in near-transparent scrim top with no text-shadow.
- (decision) resolve findings before close: tune muted palette values DARKER to hit AA while preserving Azure hues/character; correct the false AA claim; engineer hero scrim + text-shadow.
- (decision) logged-out Add-to-Trip becomes an ENABLED control that routes to login and returns (accessible best practice AND matches live DestinationDetailsPage.tsx behavior, which always renders an enabled button + login hint) rather than a disabled button; reconciles a discrepancy where epic-2 text said 'disabled' but shipped code routes-to-login. FLAGGED for user veto.
- (decision) add focus-visible token, 44px touch-target floor, prefers-reduced-motion handling, new-tab-link warning; add Responsive section (breakpoint contract) and Inspiration & Anti-patterns section (Airbnb / Google Maps / TripAdvisor / Lonely Planet references); give Flow 2 a named protagonist + climax; tokenize stray pixels; link mockups/attraction-detail-azure.html inline in both spines.
- (event) fixer resolved all reviewer findings: AA-passing palette (label-muted #586980 5.60:1, na #5e6e80 5.23:1, teal #0d7d7d 4.95:1, open-now #137a45 5.39:1, primary #1668b4 5.72:1, star glyph #b5780f 3.71:1 graphic + rating value in slate 7.51:1); engineered hero scrim + text-shadow; focus-ring token + 44px targets + reduced-motion + new-tab link; logged-out Add-to-Trip is now an enabled login-routing control; added Responsive & Platform (breakpoint 1024px) and Inspiration & Anti-Patterns sections; Flow 2 given protagonist "Dan" + climax; tokenized stray pixels; inline mock links in both spines.
- (event) validation-report.html + .md written (27 findings total: 5 crit / 5 high / 7 med / 10 low, all resolved).
- (event) editorial polish pass (structural + prose) applied; resolved a Shapes radius inconsistency (info block -> rounded/md 6px).
- (event) spines finalized: DESIGN.md and EXPERIENCE.md set status: final, updated 2026-07-16. Canonical mock promoted to mockups/attraction-detail-azure.html; directions exploration kept in .working/.
