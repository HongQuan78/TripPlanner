# Memlog — UX: Re-design Create Trip (Horizon)

topic: Re-design the create-trip frontend surface (inline form on /trips)
created: 2026-07-18
run: ux-tripplanner-2026-07-18
extends: ../ux-tripplanner-2026-07-16-trips (Boarding Pass) → ../ux-tripplanner-2026-07-15 (Horizon base)

> memlog.py unavailable (no python in env); this file is maintained by hand. Canonical decision log.

---

- [event] Run initialized. Create intent, new surface (create-trip) inside the existing Horizon / Boarding Pass design system. No prior create-trip UX run exists.
- [assumption] Inherits Horizon tokens from FE/src/app/index.css and the Boarding Pass DESIGN.md (../ux-tripplanner-2026-07-16-trips/DESIGN.md). No new hex, no new fonts unless the user asks.
- [reference] Current impl: inline CreateTripForm.tsx on /trips (TripsPage.tsx toggles it). Fields: name, startDate, endDate. DateField.tsx = react-day-picker popover per date. Validation in tripFormValidation.ts (name req, both dates req, start<=end). POST /api/trips {name,startDate,endDate}; on success navigate to /trips/:id.
- [decision] Ambition = "Elevate the inline form". Keep it inline on /trips, same 3 fields and same flow; give it a real Boarding-Pass visual treatment matching the Trips redesign. Refine-and-elevate, not re-architect.
- [decision] Redesign goals (ranked, user-selected): (1) clearer date picking, (2) visual polish / brand fit, (3) better guidance & feedback. Explicitly NOT "faster / fewer steps".
- [decision] Data scope = "Stay honest to 3 fields". Design only around name + startDate + endDate. No decorative fields AND no FE-only derived niceties (user chose the strict option over the derived-helpers option). Nothing may imply data the backend cannot store.
- [decision] Working mode = Coaching path. Walk decisions with rendered HTML mockups/wireframes before committing.
- [event] Rendered .working/directions-create-trip.html — 3 directions (A Boarding Pass Stub / B Ticket Counter / C Departure Board), differing on register and date-picking mechanism.
- [decision] Visual direction = A "Boarding Pass Stub": the create form is a blank boarding pass being issued — accent spine (left) + perforated stub (right) holding the primary CTA. Light theme only (C's dark panel rejected). Matches the Boarding Pass trips grid it sits above.
- [decision] Date picking = keep the two separate Start/End DateField controls (react-day-picker popovers, as shipped) but add range highlighting: once both dates are set, each popover highlights the in-between range and the two endpoints. NOT a single combined range calendar (smaller change from today's DateField, preserves current component architecture). This is the concrete answer to the "clearer date picking" goal.
- [assumption] Endpoints keep their existing min-date guards (start min = today; end min = start||today), now visually reinforced by the range highlight.
